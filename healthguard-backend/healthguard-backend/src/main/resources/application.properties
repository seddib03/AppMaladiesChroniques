# ===============================
# = DATABASE CONFIGURATION
# ===============================
spring.datasource.url=jdbc:postgresql://localhost:5432/healthguard_db
spring.datasource.username=postgres
spring.datasource.password=Salidb@

# ===============================
# = JPA / HIBERNATE
# ===============================
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ===============================
# = SERVER CONFIG
# ===============================
server.port=8080

# ===============================
# = LOGGING
# ===============================
logging.level.org.springframework=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# ===============================
# = TIMEZONE (optional)
# ===============================
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.ai.openai.api-key=#
spring.ai.openai.chat.options.model=gpt-3.5-turbo
spring.cloud.function.scan.enabled=false
# Désactive la détection automatique du proxy
spring.ai.openai.proxy.type=DIRECT
spring.ai.openai.client.read-timeout=60s
